import Particles from "react-tsparticles";

const Home = () => {
    // },[])

    return (
        <div className="container">
        <h1>Hello World!</h1>
        <Particles />
        </div>
    );
};

export default Home;